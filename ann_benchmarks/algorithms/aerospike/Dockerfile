FROM ann-benchmarks

ENV PROXIMUS_HOST='172.28.86.158'
ENV PROXIMUS_PORT=5000
ENV PROXIMUS_NAMESPACE='test'
ENV PROXIMUS_SET='ANN-data'
ENV VERIFY_TLS=
ENV APP_POPULATE_TASKS=5000
ENV APP_LOGFILE=
ENV API_ASLOGLEVEL=
ENV APP_LOGLEVEL=INFO

ARG AEROSPIKE_CLIENT_VERSION=0.5.1

RUN pip install pythonping
RUN pip install aerospike-vector-search==${AEROSPIKE_CLIENT_VERSION}

